<Window x:Class="FingerprintWpfDemo.EnrollWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enrol / Re-enrol" Height="350" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="User name:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtName" Width="200" Margin="10,0,0,0"/>
            <Button x:Name="btnEnrol" Content="Enrol new" Width="90" Margin="10,0,0,0" Click="btnEnrol_Click"/>
            <Button x:Name="btnReEnrol" Content="Re-enrol" Width="90" Margin="10,0,0,0" Click="btnReEnrol_Click"/>
        </StackPanel>

        <TextBlock Text="Log:" Grid.Row="1"/>

        <TextBox x:Name="txtLog" Grid.Row="2" Margin="0,5,0,0"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 IsReadOnly="True"
                 AcceptsReturn="True"/>
    </Grid>
</Window>
